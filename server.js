// server.js disabled — this folder is intended to be a static site.
// The proxy server that used to live here was removed to keep the
// dashboard directory purely static. If you need a dev proxy, recreate
// it outside this folder or run a local static server (python3 -m http.server).

console.log('server.js is intentionally disabled in the dash folder.');
